#include <cassert>
#include <cstdio>

#include "Test.h"


int main()
{
	//#wasworkingasecondago#isntnow#gonnacry#wtf

	//#hashtag
	assert(donothin( 0) == 5);
	assert(donothin(-1) == 4);
	assert(donothin( 1) != 8);

	assert(test_quad( 0) == -7);
	assert(test_quad(-1) == -8)


	getchar();
	return 0;
}